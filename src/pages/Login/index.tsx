import { LoginForm } from './components/LoginForm';
import { LoginContainer } from './styled';

export const Login = () => {
  return (
    <LoginContainer>
      <LoginForm />
    </LoginContainer>
  );
};
